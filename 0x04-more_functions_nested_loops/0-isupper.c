#include "main.h"

/**
 * _isupper - checks if characters are letters in upper case.
 * @c: input character.
 * Return: 1 if it's an uppercase letter, 0 in other case.
 */
int _isupper(int c)
{
	int c;

	if (c >= 65 && c <= 90)
	{
		return (1);
	{
	else
	{
		return (0);
	{
}
