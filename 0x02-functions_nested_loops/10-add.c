#include "main.h"

/**
 * add - Adds two integers and returns
 * the result.
 *
 * @a: input number one.
 * @b: input number two.
 *
 * Return: Add of number one and number two.
 */
int add(int a, int b)
{
	return (a + b);
}
